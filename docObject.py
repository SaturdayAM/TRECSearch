# -*- coding: utf-8 -*-
class docObject():
     """
    An object to store each document in
    (docNo, text) pairs
    
    """
    def __init__(self, number, text):
        self.number = number
        self.text = text
